# Screenshots

Rendered captures of `design_files/scheduler-kit/index.html` at 1280px viewport width.

- **`01-dashboard-top.png`** — header → alerts → 12-day projection + tank cards → next-week forecast strip → start of Schedule Parser + Auto-Planner row.
- **`02-dashboard-bottom.png`** — Schedule Parser + Auto-Planner (full) → VMI Health KPIs → VMI Controls + Trucks row → page footer.

Open `design_files/scheduler-kit/index.html` in a browser to see the live, scrollable, interactive version (controls, sliders, and the toggle work).
